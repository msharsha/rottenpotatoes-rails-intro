<!--  This file is app/views/movies/index.html.erb -->
<h2>All Movies</h2>

<%#  Part 1: Start here... %>

<table class="table table-striped col-md-12" id="movies">
  <thead>
    <tr>
      <%# Sorting Movie Titles in ascending order based on condition and adding color to it %>
      <th class="<%= @sort_items == 'movies_title' ? 'hilite bg-warning' : '' %>"><%= link_to 'Movies Title', movies_path(sort_items: "movies_title"), :id => 'title_header' %></th>
      <th>Rating</th>
      <%# Sorting Release Dates in ascending order based on condition and adding color to it %>
      <th class="<%= @sort_items == 'release_date' ? 'hilite bg-warning' : '' %>"><%= link_to 'Release Date', movies_path(sort_items: "release_date"), :id => 'release_date_header' %></th>
      <th>More Info</th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td>
          <%= movie.title %>
        </td>
        <td>
          <%= movie.rating %>
        </td>
        <td>
          <%= movie.release_date %>
        </td>
        <td>
          <%= link_to "More about #{movie.title}", movie_path(movie) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary' %>
